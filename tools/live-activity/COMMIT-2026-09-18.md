# Live Activity — portrait, Stop et périmètre du commit

Demandes de Kathryn du 18 septembre 2026 : committer uniquement ce chantier,
mettre son portrait Nosfy aux yeux rouges dans la grande carte, conserver la
lune compacte, fermer la Live Activity au Stop et documenter le tout dans Flow.
Base de préparation : `2c2c72648b46ecde9838473225cbe33b5dcf3044`.

## Contenu

- Portrait original du Bureau copié sans retouche dans `NosfyShared/LiveAssets.xcassets`,
  partagé par l’app et le widget. Il reste fixe ; son fond noir se fond dans la
  carte. Grande bannière et île déployée : portrait. Compact/minimal : lune.
- Stop lance `end(..., dismissalPolicy: .immediate)` sans attendre la file des
  updates. Les callbacks des séances arrêtées sont ignorés. Une ancienne séance
  ne peut pas arrêter une nouvelle séance. Le Stop sans modèle actif ferme aussi
  une éventuelle carte résiduelle.
- Aucun fichier partagé repris en bloc : toaster, chevrons, comptage
  `seriesPayantes`, moteurs et backend restent aux sessions propriétaires.
  `NosfyApp.swift` reçoit uniquement une ligne de fermeture dans la garde du Stop.
- Sources Flow, index d’état, contrat détaillé et livrable documentaire inclus.

## Validation et limites

Les 53 contrôles des vrais modèles et du player cardio passent. Les 16 contrôles
supplémentaires exécutent le vrai pilote avec ActivityKit doublé : Stop pendant
une update suspendue, suppression immédiate demandée, callbacks tardifs, nouvelle
séance, carte balayée et idempotence. Ce banc ne mesure pas la décision d’iOS.

Le widget avec son catalogue de portrait compile pour le simulateur. Les images
[haut de la liste](proofs/portrait/muscu-hiit.png) et
[cardio/piscine](proofs/portrait/cardio-piscine.png) montrent le vrai composant,
chargé avec le catalogue compilé du widget, dans notre application de test dédiée.
Les premières captures sans catalogue n’affichaient pas le portrait ; elles sont
invalides et remplacées. Le portrait est inspecté sans modifier le fichier source.

La compilation globale isolée est bloquée par `VolDePieces` absent dans
`HomeAuroraView.swift:196`. Le fichier est identique à la base ; la seule définition
est dans le `PlayerSeance.swift` non committé d’une autre session. Nous ne prenons
pas ce fichier : [constat](proofs/commit/baseline-home.txt). La compilation de
l’arbre partagé initial avait réussi ; elle incluait cette dépendance en cours.
La première base isolée manquait aussi du helper `nomLocalise` ; la reprise sur
le commit de langue `ed85661f` a résolu cette dépendance sans prendre ses lignes.

Le rendu système ActivityKit, la vraie Dynamic Island, le tap de reprise et le
coût thermique sur iPhone restent ouverts. Aucun accès au téléphone ni push.
La documentation du commit est reconstruite depuis ses propres sources ; le
livrable de l’arbre partagé est généré séparément pour conserver les autres travaux.


La base documentaire après les autres commits dépasse sa limite dès l’ajout de
nos encadrés. Les réserves détaillées sont regroupées dans le contrat
`docs/screens/live-activity.md`, lié depuis le Flow et les preuves de l’index ;
les lignes additionnelles QA16/mesure préparées initialement ne sont pas incluses.
Aucun contenu des autres sessions, aucune capture ni limite n’est modifié.


## Validation finale de la copie à committer

- Widget avec portrait : **BUILD SUCCEEDED**, Debug arm64 pour simulateur,
  sans installation dans l’application personnelle
  ([journal](proofs/commit/widget-build-portrait.log)).
- App isolée : défaut Home préexistant décrit ci-dessus
  ([journal](proofs/commit/build-final.log)).
- Vrais modèles partagés et player cardio : **53 contrôles PASS**, charge
  représentative de 732 octets ; pilote et Stop : **16 contrôles PASS**
  ([journal](proofs/commit/checks-stop.log)).
- Documentation du commit : artefact reconstruit, **23 tests PASS**, dix pages
  sans débordement à 390 px et captures État/Compte 390/1440 relues
  ([journal](proofs/commit/docs-verif.log)).
- Empreintes des sources Swift et du livrable :
  [sources.json](proofs/commit/sources.json).

Les avertissements d’espaces finaux de `git diff --check` concernent uniquement
les sorties brutes archivées des outils. Le contrôle des sources, hors ces
journaux `.log`, passe. Le dépôt n’a aucun hook actif de commit.
