<Decision titre="Intégrité des gains — blocage production mesuré le 18 septembre">
  <p>Sur un compte temporaire, <code>cloturer_seance</code> a crédité 20 pièces et un sachet pour une séance inexistante. Les tables restent fermées aux écritures directes ; la fonction autorisée ne valide pas suffisamment ce qu’elle rémunère. Le test <code>verif_compte.py --integrite</code> échoue et ses comptes ont été nettoyés.</p>
  <p>La RPC est signalée en rouge dans <a href="#serveur" data-saut="serveur">Serveur</a>. Son correctif devra préserver les reprises après coupure réseau ; il n’est pas déployé par ce contrôle. Preuve : <code>tools/production/preuves-2026-09-18/integrite-gains.log</code>.</p>
</Decision>

<div className="reveal">

## Ce qui ment

<Liste page="coffre" etats={['men']} vide="Rien ne ment sur cette page — le 🔴 du domaine (la home et le profil calculent chacun leur solde, b-deux-nombres) est rangé sur la carte du serveur. Le coffre disait « reste 60 » sur un solde de 1360 — treize tranches de 100 qui n'étaient des sachets nulle part : converties le 30-08 soir (20260830210000), le stock d'avant compris." />

</div>
<div className="reveal">

## Les quatre pages du coffre

<Schema id="regles-3-les-quatre-pages-du" legende="Tout vient d'un seul objet, etat_coffre(), depuis le 29-08. Depuis le 30-08 soir il convertit : à 100 pièces un sachet apparaît tout seul, le nombre monte (1, 2…), les pièces retombent — et il rend le jour, retour_disponible, retour_prochain et la flamme." />

</div>
<div className="reveal">

## Ce qui reste

<Liste page="coffre" etats={['loc', 'srv', 'abs']} mesures />

</div>
<div className="reveal">

## Ce qui tient

<Liste page="coffre" etats={['ok']} vide="Rien de branché n'est posé sur cette page : ce qui tient — le carnet, etat_coffre(), le sachet forfaitaire de clôture, la consommation du noir — est sur la carte du serveur, domaine Économie." />

</div>

<Decision titre="Tranché le 15-09">
  <p><b>Le profil, quatre pastilles sur une ligne</b> : noir, orange, argent, or — la pièce d'or sans son mot ; <b>au tap, chacune ouvre le coffre à sa page</b> (plus de manège direct depuis une pill : c'est « Ouvrir » sur la page du sachet). <b>Le géant a deux variants</b> : « N sachets à ouvrir » avec OUVRIR ; sinon <b>la jauge du coffre</b> (le même composant, « 80 / 100 ») et « Verrouillé » — plus jamais « Utiliser 100 pièces », l'achat est mort. <b>Le coffre parle la langue du profil</b>, français ou anglais, comme les autres blocs. <b>Les nombres ne bougent pas</b> (20 la série, +10 le jour) : ce qui change, c'est que l'app les <i>lit</i> au serveur partout, elle ne les <i>connaît</i> plus. Le plan : <code>tools/coffre-v2/PLAN-COFFRE-VERT.md</code>.</p>
</Decision>

<Decision titre="Tranché le 30-08">
  <p><b>La jauge convertit.</b> À 100 pièces un sachet apparaît tout seul, le nombre monte, bouton « Ouvrir » ; les pièces retombent. La conversion vit au serveur (<code>convertir_pieces()</code>, privée, déclenchée par tout crédit jaune du carnet — clôture, versement du jour, nœud du chemin), jamais dans l'écran ; le témoin d'idempotence est le solde lui-même. <code>reste = solde % prix</code> est vrai depuis la pose du 30-08 soir : le stock d'avant a été converti (compte de test : 1 536 → 15 sachets + 36), sonde <code>tools/annonces/verif_backend_coffre.py</code> verte.</p>
  <p><b>Le bouton d'achat meurt.</b> Avec la conversion, le solde ne dépasse plus 99 : « 100 coins open one » (<code>claim_booster</code>) ne peut plus réussir — la fonction est FERMÉE au serveur depuis le 30-08 soir (403) ; l'app l'appelle encore quand <code>boosters == 0</code>, retirée au J2 (Q9, défaut). « Ouvrir » ouvre un sachet qui existe déjà, et va droit au manège (Q6, défaut).</p>
  <p><b>Le profil montre tout, même à 0</b> : la pièce d'or, la pièce d'argent, le géant, le booster noir — et l'orange (Q10). <b>Le jour, c'est minuit à Paris</b> : une clé serveur <code>fuseau_jour</code>, jamais le fuseau du téléphone (Q7). <b>La flamme</b> : jours d'affilée, dérivée de <code>workouts.ended_at</code> au serveur, sans bonus, jamais stockée. Les nombres ne bougent pas : 20 / série · +10 / jour · 100 = 1 sachet · 1 argent = 1 noir · nœud 100-200. Le plan : <code>tools/annonces/PLAN-COFFRE-ANNONCES.md</code> §0, §4 M1, §5.4 — jalons J1 et J2.</p>
</Decision>

<Savoir>
  <p><b>Ce que le code faisait au 30-08 midi (relu) — corrigé le soir par <code>20260830210000_conversion_jour_flamme.sql</code>.</b> <code>etat_coffre()</code> calcule <code>reste = solde_or % prix_booster</code> sur le solde TOTAL (<code>20260830160000_sachet_scelle_et_tirage.sql:455-461</code>) — la sonde du serveur rend <code>solde_or 1360 → reste 60</code>. La migration du 29-08 l'écrit noir sur blanc : « aucune conversion automatique, et c'est voulu » (<code>20260829120000_annonces.sql:38-48</code>) — c'est ça qui a été tranché dans l'autre sens. <code>claim_booster()</code> (<code>20260828190000_gains_coffre.sql:354-381</code>) débite 100 au tap et crée un sachet <code>achat</code>, sans témoin d'idempotence (double tap = deux achats) ; on ne l'atteint que depuis le profil quand <code>boosters == 0</code> (<code>ProfilLune.swift:1255-1265</code>). La raison <code>conversion_booster</code> existe dans le check du carnet depuis le 28-08 et n'a jamais été écrite ; <code>booster_progress</code> est morte et le reste — un solde se dérive.</p>
  <p><b>Le profil</b> : les pills noire et orange sont cachées à 0 (<code>ProfilLune.swift:562-574</code>, « si supérieur à 0 »), aucune vue de la pièce d'argent (un seul commentaire, :1245), le géant est un bouton hors inventaire (:272-277). La home et le profil recalculent chacun un solde local (<code>ProfilLune.swift:127-130</code> : <code>CoffreFortPurse.coins(doneSeries:)</code>) — le 🔴 du domaine, sur la carte du serveur.</p>
  <p><b>Ce qui tient depuis le 29-08</b> : tout le coffre vient d'un seul objet, <code>etat_coffre()</code>. Avant, le même nombre était calculé à neuf endroits, et le prix d'un sachet valait 20 sur le profil, 100 dans le coffre. Le « une fois par jour » du versement tient par l'index partiel <code>coin_ledger_retour_jour_unique</code>, pas par un réglage — l'idempotence est un index, jamais un compteur. Son rythme et sa porte sont sur « Les annonces ».</p>
  <p><b>La flamme</b> : au serveur depuis le 30-08 soir (<code>flamme()</code>, dérivée de <code>workouts.ended_at</code> dans le fuseau de la maison, aussi dans <code>etat_coffre().flamme</code>), personne ne l'affiche encore (J2). Avant : rien, nulle part — ni fonction, ni clé, ni colonne ; le seul « streak » est hebdomadaire, local, dans <code>CalendarView</code> que personne ne monte ; la flamme des galets est un glyphe (plan §2.5).</p>
</Savoir>
