<Decision titre="Stories restaurées — reçus retrouvés, 19 septembre">
  <p>La lecture seule <code>recus_seances</code> retrouve pièces, sachets, argent et faits TOP/double. Elle complète le pull neuf et les séances déjà téléchargées sans reçu. Les anciens gains restent lisibles dans leurs écritures conservées ; revoir une story ne paie rien et ne rejoue aucune annonce.</p>
  <p>24 contrôles Swift connectés, 17 API et 17 tests du pull réussis : bilans identiques après acquittement et ouverture, reprise après panne, isolation entre comptes. Accès ajouté depuis les jours faits du widget Regularity et sa grille ; plusieurs séances le même jour donnent un choix.</p>
  <p>Preuves et limites : <code>tools/production/stories-historique-2026-09-19/README.md</code>. Aucun téléphone utilisé.</p>
</Decision>

<Decision titre="Parcours de fin de séance — 19 septembre">
  <p>Les séries prévues ne gonflent plus le résumé ni le volume. Pièces et sachets viennent du reçu de cette séance, conservé sur le téléphone avec TOP/double. Une réponse absente dit « Récompenses en attente ». Le cardio ne reçoit plus de séries de démonstration. Les kcal restent une estimation locale.</p>
  <p>Preuves et limites : <code>tools/production/fin-seance-2026-09-19/README.md</code>. Contrôles iPhone, Apple et chauffe encore ouverts dans Test QA.</p>
</Decision>

<div className="reveal">

## Ce qui ment

<Liste page="histoire" etats={['men']} vide="Les stories lisent les faits et les gains confirmés, sans nouveau paiement." />

</div>
<div className="reveal">

## Comment on choisit un variant

<Schema id="histoire-1-comment-on-choisit-un" legende="Tout est en trait plein depuis le 17-09 : le serveur calcule le record à la clôture (calculer_faits_seance) et la story LIT ces faits pour choisir sa robe — la détection existe, ce n'est plus un drapeau de banc." />

</div>
<div className="reveal">

## Les variants, en images

<Story />

</div>
<div className="reveal">

## Ce qui reste

<Liste page="histoire" etats={['loc', 'srv', 'abs']} mesures />

</div>
<div className="reveal">

## Ce qui tient

<Liste page="histoire" etats={['ok']} />

</div>

<Savoir titre="Tranché le 15-09 — le variant vit au serveur">
  <p>Un fait est un <b>estampillage</b> : posé une fois à la clôture (<code>calculer_faits_seance</code>, index <code>workout_facts_unique</code>), jamais recalculé — une séance a sa robe pour toujours, un record « battu » ne redevient pas un jour ordinaire. La question « où vit le variant » n'est plus à trancher : le serveur décide, la story lit <code>faits</code> dans la réponse de clôture.</p>
</Savoir>

<Savoir>
  <p>Le rangement des faits est posé (30-08) : la table <code>workout_facts</code> et <code>poser_faits_seance</code>. Le CALCUL est au serveur depuis le 15-09 (<code>calculer_faits_seance</code>, appelée par <code>cloturer_seance</code>) : le 30-08 avait tranché « l'app calcule » faute de séances au serveur — elles y sont depuis le 13-09, et la clôture arrive après la poussée.</p>
  <p>La cause unique des « local » — <code>SupabaseSync</code> n'avait qu'un <code>push</code> — est levée depuis le 14-09 (le pull, <code>seances_depuis</code>). La story, elle, ne dérive plus rien de SwiftData : depuis le 17-09 elle lit « ce jour-là a été un record » directement dans la réponse de clôture (<code>faits</code>, b-st-top), estampillée par le serveur — un téléphone neuf ouvre donc la bonne robe sans avoir rejoué tout l'historique. Le calendrier et le chemin lisent les séances restaurées ; les reçus viennent de leur lecture dédiée. L'audit des trois contradictions : <code>tools/road/AUDIT-CHEMIN-CALENDRIER-STORIES.md</code>.</p>
</Savoir>
