<div className="reveal">

## Ce qui ment

<Liste page="porte" etats={['men']} />

</div>
<div className="reveal">

## Ce que la porte fait vraiment

<Schema id="porte-1-ce-que-la-porte" legende="Il y a bien un onboarding — quatre pages, un film d'arrivée de 5,47 s. Ce qui manque, c'est ce qui vient après le bouton." />

</div>
<div className="reveal">

<Onboarding />

</div>
<div className="reveal">

## Ce qui reste

<Liste page="porte" etats={['loc', 'srv', 'abs']} mesures />

</div>
<div className="reveal">

## Ce qui tient

<Liste page="porte" etats={['ok']} />

</div>
<div className="reveal">

## Le test de bout en bout — validé ensemble, front et back

<ParcoursQA compact />

<p>Le détail de chaque étape (le geste, ce qu'elle voit, ce que le serveur tient, les deux verdicts) vit dans l'onglet <a href="#qa"><b>Test QA</b></a>. Le 14-09 : le vrai binaire posé sur son iPhone (téléphone désinstallé avant, compte serveur remis à zéro, ses 71 séances exportées), le script joué par elle ; les verdicts se posent au fur et à mesure.</p>

</div>

<Decision titre="Tranché le 13-09 — et ce qui reste">
  <p><b>Tu as tranché</b> : on n'entre que par Apple, sans OTP ni lien magique ; un compte existant arrive dans l'app ; « Se déconnecter » ramène à l'écran de login. Donc : pas de mode invité — la question « dire qu'on n'est pas connectée ? » n'existe plus, et « migrer la maquette locale » (rattacher au compte des pièces gagnées sans compte) n'a plus rien à migrer. Le plan des chantiers, dans l'ordre : <code>tools/porte/PLAN-COMPTE-BACKEND.md</code>.</p>
  <p><b>Le « go » est donné (13-09, 22:00)</b>, les défauts pris tels quels : à la déconnexion on pousse puis on efface le téléphone ; la suppression est immédiate ; la porte e-mail reste pour le banc ; les 7 comptes fantômes sont effacés (fait). <b>Une action pour toi</b> : la clé <code>.p8</code> Sign in with Apple (portail développeur → Keys, cocher Sign in with Apple, télécharger une seule fois ; me donner le Key ID et le Team ID). Sans elle, la suppression efface le compte mais ne peut pas révoquer chez Apple, et <code>apple-jeton</code> répond <code>cle_absente</code>.</p>
  <p><b>« Peut-on créer des utilisateurs qui s'inscrivent et bénéficient de l'expérience ? »</b> (ta question du 13-09, 23:40). <b>Côté serveur, oui</b> : compte par Apple, profil, séances, widgets, home, suppression — tout est mesuré, il ne manque que ta clé Apple, exigée à la revue App Store, pas avant. <b>Côté app, depuis le 14-09 : oui aussi</b> — sur ton ordre (« fais le backend manquant et ajoute dans l'app ce qu'il manque »), la session back-end a posé et mesuré C0 (session gardée → la home direct, la porte seulement sans session), C2 (déconnexion vraie, refresh au Keychain), C3-app (suppression vraie ; l'<code>authorizationCode</code> d'Apple part à <code>apple-jeton</code>) et C4 (les numéros hors du binaire, le prénom du profil dans la page profil) ; le pull est fait, la langue est lue, la visite « la brume » est posée. <b>Ce qui reste</b> : ta clé <code>.p8</code>, et le vrai test de bout en bout sur ton téléphone (ton script : porte → Apple → création → Nosfy → home vide dans ta langue → pop-up et visite → profil à ton prénom → relance sans porte → déconnexion → suppression), plus la mesure de la première arrivée sur un compte jetable (session porte). Le détail : <code>tools/porte/PLAN-COMPTE-BACKEND.md</code> § 7 et § 9.</p>
  <p><b>Ta règle du 14-09, dans le schéma</b> : <b>aucun Welcome Back</b> pendant la porte, la création du compte, le film de Nosfy ni la première arrivée — il n'existe qu'à un compte créé qui a fini sa première séance, une fois par jour de la maison. Deux verrous : le serveur (<code>retour_disponible</code> faux sans séance finie, claim refusé) et l'app (<code>Compte.proposerWelcomeBack</code>, jamais sous la porte, le film ou le splash, jamais en première fois). Mesuré le 14-09, le journal <code>[welcome-back]</code> dit à chaque fois pourquoi.</p>
</Decision>

<Savoir>
  <p>La session tient désormais à la porte Apple : <code>AppleAuth.entrer</code> échange le jeton d'identité contre une session Supabase (<code>adopter</code>), lit <code>profil()</code> et aiguille — connue → la home, nouvelle → le film de Nosfy, qui finit par <code>definir_profil</code> (prénom obligatoire). L'ancienne clé <code>woop.phone</code> est morte (14-09, C4) : le banc entre par <code>-sessionBanc</code> / <code>-sessionAdoptee [email mdp]</code>, sous DEBUG seulement — en release, Apple seul.</p>
  <p>Ce qui manque, lu le 13-09 au soir : la porte se rejoue à chaque lancement même avec une session ; « Se déconnecter » n'efface que <code>woop.phone</code> ; « Supprimer mon compte » ne supprime rien ; le jeton vit dans les préférences, pas dans le Keychain ; et Supabase accepte encore des comptes anonymes. Depuis le « go » du 13-09 soir, le serveur a fait sa part : les portes sont fermées, la première arrivée a sa mémoire et sa règle (Welcome Back muet avant la première séance), et la suppression existe (<code>supprimer-compte</code>, mesurée sur un compte jetable) avec <code>apple-jeton</code> qui attend la clé .p8. Le côté app est fait et mesuré le 14-09 (<code>Compte.swift</code>) : la porte seulement sans session — une session gardée (identité Apple + refresh au Keychain) ouvre la home direct, un refresh refusé rend la porte sur un téléphone vierge ; « Se déconnecter » pousse ce qui attend (refus hors ligne), révoque au serveur, efface tout ce qui est à elle (clés, séances, chambre, économie) et rend la porte ; « Supprimer mon compte » appelle <code>supprimer-compte</code> puis fait de même ; les numéros en dur sont sortis du binaire ; la page profil dit le prénom du profil. La lecture des séances est complète (<code>seances_depuis</code> + le pull dans l'app, mesuré : un téléphone neuf retrouve ses 23 séances). Et les révocations Apple ratées se rejouent toutes seules (<code>rejouer-revocations</code>, et au passage à chaque entrée ou suppression).</p>
  <Schema id="porte-2-pourquoi-ca-commande-tout" titre="Pourquoi ça commande tout le reste" plie />
  <Schema id="porte-3-le-parcours-cible" titre="Le parcours cible" plie />
</Savoir>
