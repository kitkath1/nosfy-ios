<div className="reveal">

## Ce qui ment

<Liste page="porte" etats={['men']} />

</div>
<div className="reveal">

## Ce que la porte fait vraiment

<Schema id="porte-1-ce-que-la-porte" legende="Il y a bien un onboarding — quatre pages, un film d'arrivée de 5,47 s. Ce qui manque, c'est ce qui vient après le bouton." />

</div>
<div className="reveal">

<Onboarding />

</div>
<div className="reveal">

## Ce qui reste

<Liste page="porte" etats={['loc', 'srv', 'abs']} mesures />

</div>
<div className="reveal">

## Ce qui tient

<Liste page="porte" etats={['ok']} />

</div>

<Decision titre="Tranché le 13-09 — et ce qui reste">
  <p><b>Tu as tranché</b> : on n'entre que par Apple, sans OTP ni lien magique ; un compte existant arrive dans l'app ; « Se déconnecter » ramène à l'écran de login. Donc : pas de mode invité — la question « dire qu'on n'est pas connectée ? » n'existe plus, et « migrer la maquette locale » (rattacher au compte des pièces gagnées sans compte) n'a plus rien à migrer. Le plan des chantiers, dans l'ordre : <code>tools/porte/PLAN-COMPTE-BACKEND.md</code>.</p>
  <p><b>Le « go » est donné (13-09, 22:00)</b>, les défauts pris tels quels : à la déconnexion on pousse puis on efface le téléphone ; la suppression est immédiate ; la porte e-mail reste pour le banc ; les 7 comptes fantômes sont effacés (fait). <b>Une action pour toi</b> : la clé <code>.p8</code> Sign in with Apple (portail développeur → Keys, cocher Sign in with Apple, télécharger une seule fois ; me donner le Key ID et le Team ID). Sans elle, la suppression efface le compte mais ne peut pas révoquer chez Apple, et <code>apple-jeton</code> répond <code>cle_absente</code>.</p>
  <p><b>« Peut-on créer des utilisateurs qui s'inscrivent et bénéficient de l'expérience ? »</b> (ta question du 13-09, 23:40). <b>Côté serveur, oui</b> : compte par Apple, profil, séances, widgets, home, suppression — tout est mesuré, il ne manque que ta clé Apple, exigée à la revue App Store, pas avant. <b>Côté app, pas encore pour des inconnus</b> : la porte se rejoue à chaque ouverture (C0), déconnexion et suppression ne font rien de réel (C2, C3), le jeton dort dans les préférences et les deux numéros en dur sont encore dans le binaire (C4), la langue choisie n'est pas lue, la visite v2 est en plan, et le vrai chemin Apple n'a pas été re-mesuré depuis l'aiguillage par <code>profil()</code>. Entre les deux, le pull des séances (moitié serveur faite). Pour toi et des testeurs proches : dès C0 + C2 + C3 + C4. Le détail : <code>tools/porte/PLAN-COMPTE-BACKEND.md</code> § 7.</p>
</Decision>

<Savoir>
  <p>La session tient désormais à la porte Apple : <code>AppleAuth.entrer</code> échange le jeton d'identité contre une session Supabase (<code>adopter</code>), lit <code>profil()</code> et aiguille — connue → la home, nouvelle → le film de Nosfy, qui finit par <code>definir_profil</code> (prénom obligatoire). L'ancienne clé <code>woop.phone</code> ne sert plus qu'au banc (<code>-sessionBanc</code>, le compte de test par e-mail) et disparaît avec le chantier C4 du plan.</p>
  <p>Ce qui manque, lu le 13-09 au soir : la porte se rejoue à chaque lancement même avec une session ; « Se déconnecter » n'efface que <code>woop.phone</code> ; « Supprimer mon compte » ne supprime rien ; le jeton vit dans les préférences, pas dans le Keychain ; et Supabase accepte encore des comptes anonymes. Depuis le « go » du 13-09 soir, le serveur a fait sa part : les portes sont fermées, la première arrivée a sa mémoire et sa règle (Welcome Back muet avant la première séance), et la suppression existe (<code>supprimer-compte</code>, mesurée sur un compte jetable) avec <code>apple-jeton</code> qui attend la clé .p8. Reste le côté app, chez la session porte : la porte seulement sans session, la déconnexion vraie, l'appel de la suppression, les numéros en dur. La lecture des séances a désormais sa moitié serveur (<code>seances_depuis</code>, l'arbre complet, mesuré : 23 séances, 43 exercices, 88 séries, 70 phases, les mêmes uuid que le téléphone a poussés) ; reste le pull dans l'app, chantier à part. Et les révocations Apple ratées se rejouent toutes seules (<code>rejouer-revocations</code>, et au passage à chaque entrée ou suppression).</p>
  <Schema id="porte-2-pourquoi-ca-commande-tout" titre="Pourquoi ça commande tout le reste" plie />
  <Schema id="porte-3-le-parcours-cible" titre="Le parcours cible" plie />
</Savoir>
